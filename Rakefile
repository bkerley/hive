require 'rubygems'
require 'hoe'
require './lib/hive.rb'

Hoe.new('hive', Hive::VERSION) do |p|
  p.author = 'Bryce Kerley'
  p.email = 'bryce@worldmedia.net'
  p.summary = 'Versioned YAML-backed database'
  p.description = "Hive was designed for centralized storage of user data orthogonal but related to the shove\_auth centralized authentication system.  It uses git (through grit) to provide versioning and change tracking."
#  p.url = p.paragraphs_of('README.txt', 0).first.split(/\n/)[2..-1].map { |u| u.strip }
#  p.changes = p.paragraphs_of('History.txt', 0..1).join("\n\n")
#  p.extra_deps << ['mime-types']
end